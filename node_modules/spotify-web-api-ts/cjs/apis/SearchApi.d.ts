import { Http } from '../helpers/Http';
import { Artist, Paging, SearchType, SimplifiedAlbum, SimplifiedEpisode, SimplifiedPlaylist, SimplifiedShow, Track } from '../types/SpotifyObjects';
import { SearchOptions } from '../types/SpotifyOptions';
import { SearchResponse } from '../types/SpotifyResponses';
export declare class SearchApi {
    private http;
    constructor(http: Http);
    search(query: string, type: SearchType[], options?: SearchOptions): Promise<SearchResponse>;
    searchAlbums(query: string, options?: SearchOptions): Promise<Paging<SimplifiedAlbum>>;
    searchArtists(query: string, options?: SearchOptions): Promise<Paging<Artist>>;
    searchEpisodes(query: string, options?: SearchOptions): Promise<Paging<SimplifiedEpisode>>;
    searchPlaylists(query: string, options?: SearchOptions): Promise<Paging<SimplifiedPlaylist>>;
    searchShows(query: string, options?: SearchOptions): Promise<Paging<SimplifiedShow>>;
    searchTracks(query: string, options?: SearchOptions): Promise<Paging<Track>>;
}
