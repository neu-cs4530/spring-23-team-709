import { Http } from '../helpers/Http';
import { Artist, Track } from '../types/SpotifyObjects';
import { GetArtistAlbumsOptions } from '../types/SpotifyOptions';
import { GetArtistAlbumsResponse } from '../types/SpotifyResponses';
export declare class ArtistsApi {
    private http;
    constructor(http: Http);
    getArtist(artistId: string): Promise<Artist>;
    getArtistAlbums(artistId: string, options?: GetArtistAlbumsOptions): Promise<GetArtistAlbumsResponse>;
    getArtists(artistIds: string[]): Promise<Artist[]>;
    getArtistTopTracks(artistId: string, country: string): Promise<Track[]>;
    getRelatedArtists(artistId: string): Promise<Artist[]>;
}
